apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    url: http://prometheus:9090
    basicAuth: false
    isDefault: true
    editable: true
  # - name: Loki
    # type: loki
    # access: proxy
    # url: http://loki:3100
    # jsonData:
      # maxLines: 1000
    # basicAuth: false
    # editable: true
  # - name: PostgreSQL
    # type: postgres
    # url: postgres:5432
    # database: adc
    # user: mirero
    # secureJsonData:
      # password: "system"
    # jsonData:
      # sslmode: "disable" # disable/require/verify-ca/verify-full
      # maxOpenConns: 0         # Grafana v5.4+
      # maxIdleConns: 2         # Grafana v5.4+
      # connMaxLifetime: 14400  # Grafana v5.4+
      # postgresVersion: 1404 # 903=9.3, 904=9.4, 905=9.5, 906=9.6, 1000=10
      # timescaledb: false
    # basicAuth: false
    # editable: true